const supabase=window.supabaseClient,form=document.getElementById("reset-form"),status=document.getElementById("reset-status"),button=document.getElementById("reset-button");form?.addEventListener("submit",async e=>{e.preventDefault();button.disabled=true;status.textContent="Enviando…";try{const email=document.getElementById("email").value.trim().toLowerCase();const {error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:new URL("update-password.html",location.href).href});if(error)throw error;status.textContent="Si existe una cuenta con ese correo, recibirás un enlace para cambiar la contraseña.";status.className="form-status success"}catch(err){status.textContent="No fue posible enviar el enlace. Inténtalo de nuevo.";status.className="form-status error"}finally{button.disabled=false}});